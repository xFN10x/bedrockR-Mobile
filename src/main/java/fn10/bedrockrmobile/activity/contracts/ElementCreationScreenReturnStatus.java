package fn10.bedrockrmobile.activity.contracts;

public enum ElementCreationScreenReturnStatus {
    CREATED(0),
    DRAFTED(1),
    CANCELED(2);

    ElementCreationScreenReturnStatus(int i) {
    }
}
